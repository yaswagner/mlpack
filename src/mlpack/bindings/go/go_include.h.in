#include <stdint.h>
#include <stddef.h>

#if defined(__cplusplus) || defined(c_plusplus)
extern "C" {
#endif

extern void *MLPACK_${PROGRAM_NAME}_modelPtr();

extern void MLPACK_${PROGRAM_NAME}();

#if defined(__cplusplus) || defined(c_plusplus)
}
#endif
