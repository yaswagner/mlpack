#include "${PROGRAM_NAME}_impl.hpp"
#include "${PROGRAM_NAME}.hpp"
#include "${PROGRAM_NAME}_main.cpp"
#include "${PROGRAM_NAME}.h"

namespace mlpack {

static void ${PROGRAM_NAME}_mlpackMain()
{
  mlpackMain();
}

extern "C" void MLPACK_${PROGRAM_NAME}_mlpackMain()
{
  ${PROGRAM_NAME}_mlpackMain();
}

} // namespace mlpack
